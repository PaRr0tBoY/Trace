# 13 — 事件归属器

**What to build:** 前台事件开始"喂"任务：切换到的应用匹配任务 AppRef 时，该任务收到归属事件（lastActiveAt 更新、Paused 自动恢复）；多个任务共享同一应用时归属给最近活跃者；AppRef.lastContext 快照随事件刷新。

**Blocked by:** 11（任务域核心）、12（L0 事件采集）

**Status:** resolved

- [ ] AppRef 匹配：exePath 小写归一化作 key，无 exePath 时用进程名；归属只更新 lastActiveAt 与状态机，不改变任务标题/内容
- [ ] 多任务共享同一应用：归属 lastActiveAt 最近者，不强行打断其他任务
- [ ] lastContext 快照更新（windowTitle 等 L0 字段）
- [ ] 与 11 的状态机联动：归属事件 → Paused 自动恢复 Active
- [ ] vitest：匹配判定、三任务共享一应用的消歧、状态机联动（注入时钟）；typecheck + npm test 全绿

## 参考

规格《实现决策 2》状态机归属语义、《实现决策 3》数据流（事件总线 → 归属器）。

## Resolution

Implemented f5974cf (13 = 13-事件归属器); wave-verified by coordinator (typecheck + vitest green). t12 additionally got a koffi OpenProcess fix (a5fe873, runtime smoke-test catch). t20 split across t20a (MemoryStore, c307870) + t20b (integration, 7510a92). Final regression: 308 tests pass, build ok, startup smoke clean.
