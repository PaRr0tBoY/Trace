# 55 — 当前任务控制器（主 seam）

**What to build:** 决策层的心脏：`CurrentTaskController.observe(activity)` 作为主测试 seam。Task Change Detector 六触发（分数跌破阈值 / 候选超 margin / 新语义簇 / 长 idle 恢复 / 多任务竞争 / 会话边界）门控决策调用——99% 普通状态 0 次 LLM；滞回（候选持续 30–60s + 分数差 ≥ margin 才切换，参数进设置）；输出决策 / 会话切换 / 状态迁移 / trace 追加；决策者可替换（Agent / 本地模型 / 纯算法实现同一接口，控制器不感知谁在做）。

**Blocked by:** 36, 37, 38, 40, 41

**Status:** ready-for-agent

- [ ] 门控：稳定连续活动 → 决策者 0 次调用（99% 不变量）；分数跌破阈值 → 恰好 1 次；多候选竞争 → 升级路径
- [ ] 滞回：候选持续不足 30–60s 不切换；满足 threshold + margin + dwell 才 switch
- [ ] 切换原子：旧会话结算 + 新会话开启 + 状态迁移一次完成；PAUSED 免疫（决策说 switch 到 PAUSED 任务被拒）；runningTaskCount ≤ 1
- [ ] 决策结果全部追加 trace（可追溯，不变量 I）
- [ ] 决策者注入：agent / 算法 / 本地模型（fake）产出一致结构；本地模型失败 → 算法候选原样
- [ ] vitest：上述用例（spec 主 seam 清单）；typecheck + npm test 全绿

## 参考

spec 实现决策 5（主 seam、六触发、滞回）与 Testing Decisions 主 seam 用例；CONTEXT.md 当前任务词条。
