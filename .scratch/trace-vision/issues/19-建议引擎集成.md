# 19 — 建议引擎集成

**What to build:** 静默期后出现可交互建议卡：事件批 → 16 的管线判定归属 → LLM 标注（走 15，未配置/失败时静默降级为纯算法）→ 卡片显示可编辑标题/应用 chips/置信度/可展开原因；[确认] merge 候选任务或新建、[编辑] 改标题后确认、[忽略] 签名入表同类不再出现。

**Blocked by:** 15（AI Provider 层）、16（聚类管线）、17（任务层 UI）

**Status:** resolved

- [ ] 触发：事件缓冲 ≥5 条新事件（可配）+ 静默 ≥60s（可配 30-300）→ 触发一次分析；建议卡不打断（任务层顶部 + 状态徽标）；参数进设置（三处登记）
- [ ] 管线接线：规则预筛 + 16 的段化/归属/置信度输出 → 建议对象（含算法证据）
- [ ] LLM 辅助边界：只做标题生成、原因解释、低置信标注，不承担归属判定；建议卡原因 = 算法证据（可展开）+ LLM 通俗理由，保留关键词级证据供核对
- [ ] 交互：确认 → merge 候选任务（11 的 merge，类型安全）或新建；编辑 → 改标题后确认；忽略 → 丢弃 + 签名入 ignored.json（userData，LRU 200）；suggestion 瞬态内存不持久化
- [ ] 降级：未配置 provider / 全链失败 → 建议层静默降级（仅算法），任务系统照常
- [ ] vitest：触发条件（注入时钟）、忽略去重（签名查重 + LRU 驱逐）、确认 merge/新建分流、降级路径；typecheck + npm test 全绿；dev 手动验证（真实静默后出卡）

## 参考

规格《实现决策 5》AI 建议引擎、《实现决策 6》Provider 降级语义。

## Resolution

Implemented 0d70b95 (19 = 19-建议引擎集成); wave-verified by coordinator (typecheck + vitest green). t12 additionally got a koffi OpenProcess fix (a5fe873, runtime smoke-test catch). t20 split across t20a (MemoryStore, c307870) + t20b (integration, 7510a92). Final regression: 308 tests pass, build ok, startup smoke clean.
