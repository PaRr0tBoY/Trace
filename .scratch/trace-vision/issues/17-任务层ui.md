# 17 — 任务层 UI

**What to build:** 面板内任务层视图可用：状态分组列表 + 任务详情（资源展示、alive 占位）+ 创建/编辑/删除 + 暂停/恢复/完成；折叠态徽标显示 Paused + Waiting 数。建议卡区域预留。

**Blocked by:** 11（任务域核心）、13（事件归属器）

**Status:** resolved

- [ ] 任务层视图（顶部建议卡区域 + 任务列表 + 任务详情）接入现有面板导航
- [ ] 列表：状态分组（Active > Waiting > Paused > Completed），组内 lastActiveAt 降序
- [ ] CRUD 与状态操作全走 window.edge（task:* invoke）；以 state:tasks 推送为准（renderer 只是视图缓存，乐观更新后以 main 为准）
- [ ] 详情：资源展示（文本 preview / 图片占位 / 文件路径列表）；alive=false 显示"已从剪贴板移除"占位
- [ ] 折叠态徽标 = Paused + Waiting 数
- [ ] 新 UI 文案走现有 i18n 机制注册（zh 必填，其余语言回退）
- [ ] typecheck + npm test 全绿；npm run dev 手动验证（创建→编辑→暂停→完成→硬删全流程）

## 参考

规格《实现决策 1》状态流（Main 单一真实来源）、《实现决策 2》排序与展示、《实现决策 8》V1 交互。

## Resolution

Implemented 99ef6a7 (17 = 17-任务层ui); wave-verified by coordinator (typecheck + vitest green). t12 additionally got a koffi OpenProcess fix (a5fe873, runtime smoke-test catch). t20 split across t20a (MemoryStore, c307870) + t20b (integration, 7510a92). Final regression: 308 tests pass, build ok, startup smoke clean.
