# Trace

悬停展开的剪贴板管理器。本文件是领域术语表（ubiquitous language）：覆盖任务层及其关联物——提案（UI 候选任务）、任务、任务会话、关联内容、关联应用与关联窗口、活动、证据时间线——以及面板与 AI 层的核心概念（工作上下文、Agent、记忆）。不承载实现细节、规格或决策；实现决策见 `docs/adr/`，规格见 `.scratch/trace-vision/spec.md`。

## Language

**面板（Panel）**:
悬停屏幕边缘展开的浮层，Trace 的主界面。技术上是不抢焦点的透明窗口。
_Avoid_: 主界面、窗口

**剪贴板栈（Shelf）**:
剪贴板内容的有序收藏栈（去重、置顶、持久化）。
_Avoid_: 中转站、保存区

**保存区（Drop Bar）**:
拖入面板时顶部固定的落点，拖入即存入剪贴板栈，不挂接任务。
_Avoid_: 顶栏、落点区

**任务（Task）**:
一个有焦点的工作单元，由用户创建或提案采纳而来；有状态机：RUNNING（唯一，系统判定正在进行的任务）/ WAITING（自动休息态）/ PAUSED（用户手动暂停，免疫自动恢复）/ COMPLETED（用户明确完成）/ ARCHIVED（生命周期结束）。状态标注来源（user 手动 / system 推断）与原因，UI 可区分"你暂停了"与"系统判断等待中"。
_Avoid_: 已有任务（口语）、工作项

**任务会话（TaskSession）**:
任务的一次连续运行实例：startedAt / endedAt / 置信度 / 切换原因 / 前一任务。一个任务有多个会话；全局同时只有一个会话在运行（runningTaskCount ≤ 1，由任务状态控制器强制）。会话是其窗口内多个活动的**容器**——活动是观察对象，会话是解释对象，两者 1:N 不一一对应。
_Avoid_: 时间段、活动段

**当前任务（Current Task）**:
系统判定用户此刻正在进行的任务——全局唯一 RUNNING。由任务状态控制器维护，Agent 只提案不直接改状态。
_Avoid_: 活跃任务

**活动（Activity）**:
聚类模块对一段行为证据的分析产物：应用集、窗口标题、活跃时长、剪贴板材料与归属目标；是 Agent 拼装提案的输入积木。**观察对象**（所属任务会话是解释对象）。不入 UI、不持久化（持久化的是采集事件与整理后的 episode）。记录版本信息（classifierVersion / promptVersion）供追溯。
_Avoid_: 片段、事件段、时段

**证据时间线（Evidence Timeline）**:
采集事件（应用切换、剪贴板复制）的持久化历史，带时间与来源；供 Agent 查询"用户之前做了什么"。
_Avoid_: 历史、日志

**提案（TaskProposal）**:
Agent 依据活动与记忆推出的待采纳任务建议（标题、理由、关联应用/窗口、剪贴板材料、证据与置信度）；采纳后成为任务或并入已有任务。UI 上称"候选任务"（ADR-0004），建议卡即其展示。代码原名 Suggestion。
_Avoid_: 备选任务、建议任务、建议

**任务决策（TaskDecision）**:
决策协议（evaluateTaskContext）的一次输出：action 为 continue（继续当前任务）/ switch（切换）/ new（新建）/ merge（并入已有任务）/ ignore（忽略），附目标任务、标题、理由与置信度。Agent、本地模型、纯算法都实现同一接口，外层不感知决策者；状态转换由确定性的状态控制器验证后执行。决策链以决策前创建的 decisionId 贯穿（预填隐私拦截、工具召回、决策与提案同链审计）；new/merge 产出待采纳提案（≤3 未决），ignore 记推荐历史（L3、同类冷却）。`decidedBy` 标识实际做出判断的决策者层（AI 失败/关闭时降级为算法）。
_Avoid_: 判断、结论

**采纳（Adopt）**:
把候选任务确认为任务的动作。
_Avoid_: 接受、确认

**关联内容（Resource）**:
附着在任务上的素材：剪贴板条目（文本/图片）或文件列表。任务只保存内容的预览快照，不保存全文；链接时快照，条目被逐出后降级为"失效占位"。
_Avoid_: 资源、附件、素材

**关联应用（App）**:
任务关联的应用，按最近使用排序；身份键为归一化 exePath，取不到时用进程名。
_Avoid_: 应用引用、AppRef

**关联窗口（Linked Window）**:
应用首次加入任务时记录的前台窗口快照（进程 + 标题 + 时刻）。任务详情"打开应用"按钮切换到该窗口；窗口已关但应用还在时切到应用现有窗口；应用也不在时启动它。
_Avoid_: 来源窗口、目标窗口

**运行时长（Active Duration）**:
任务累计处于 RUNNING 状态的时长，离开 RUNNING 时结算；暂停与等待时间不计入。
_Avoid_: 运行时间、活跃时长

**主题色（Theme Color）**:
面板 UI、拖拽幽灵与复制指示器的主色。五主题：Graphite 石墨灰（默认）、Cobalt 钴蓝、Verdigris 铜绿、Amber 琥珀、Violet 紫罗兰。
_Avoid_: 强调色、主题

**任务详情（Task Detail）**:
任务卡片的二级视图：关联应用（含打开应用按钮）、窗口标题、关联内容、置信度与创建原因。
_Avoid_: 二级视图、详情页

**文件成员（File Member）**:
文件条目（组）内的单个路径；文件二级视图按成员展示而非按组。
_Avoid_: 子文件、单文件卡

**拖出（Drag Out）**:
把关联内容或剪贴板条目拖到面板外其他应用的操作；文本以文本形式拖出，图片与文件以文件形式拖出。
_Avoid_: 拖拽（泛指）

**绑定（Link）**:
把内容挂到某任务上、形成关联内容的动作；拖到任务卡或绑定面板的落点都算。关联内容只由显式绑定/解绑变更，不随复制自动变化。
_Avoid_: 挂接（旧自动归属术语）

**置信度（Confidence）**:
对"这段活动属于某任务"的信心：以聚类算法为基础，Agent 可依记忆修正。三区：高（自动建议）/低（待确认）/新（新任务候选）。
_Avoid_: 信心、分数

**OCR 内容（OCR Context）**:
从窗口/图片识别的文字。只作为 AI 建议判断的输入资料，不直接展示给用户，不持久化。

**用户画像（Profile）**:
用户在设置里自述的职业与自我介绍，随上下文预填给 Agent，辅助生成更贴切的标题与理由。
_Avoid_: 身份、个人资料

**本地模型（Local Model）**:
嵌入产品内的可选小型语言模型（Qwen3-0.6B），在聚类与 Agent 之间做候选过滤、标题草稿与排序；可关闭，失败自动降级，不依赖外部服务。
_Avoid_: 本地 Ollama、本地大模型

**Agent 循环（Agent Loop）**:
Agent 层在**高不确定场景**（候选竞争、疑似新主题、置信跌破低阈值等）的确定性升级路径：按固定顺序发起 ≤3 次工具查询（search_tasks / search_memories / search_activities / search_clipboard，四固定面，无 getEverything 类工具；剪贴板最敏感、只随引用出现、仅预览且受剪贴板访问开关控制，关 → 空结果并记拦截），结果与拦截/失败行一并进入**一次性**任务决策请求（ChatFn 单次契约，非模型驱动的多轮 ReAct），预算耗尽即停。稳态直接走最小预填决策，不升级。
_Avoid_: 工具调用、多轮对话

**工作上下文（Work Context）**:
AI 观测用户工作的"横切面"：某时刻的多维切片——当前活动、当前任务与会话、召回的记忆/任务/剪贴板、证据分级（L0 元数据 → L1 结构化 → L2 语义 → L3 视觉 → L4 历史）与关系。Agent 的预填与工具都作用于它；预填整体过隐私门（不变量 D：denied 应用剥离活动/窗口/组合、memoryAccess 关 → 无记忆、aiEnabled 关/时间窗外 → 整趟算法兜底；被拒数据落 trace kind='privacy'）。
_Avoid_: 上下文（泛指）、横切面（口语）

**证据分级（Evidence Level）**:
AI 获取信息的深度维度（L0 元数据 / L1 结构化 / L2 语义 / L3 视觉 / L4 历史）。与隐私敏感度是**两个正交维度**：一张截图可以是 L3 深度但 privacy=denied。
_Avoid_: 隐私等级

**记忆（Memory）**:
长期上下文图，只有三类表：**episode / entity / fact**。画像、模式、任务记忆、偏好等概念一律作为 fact 的 type / source 字段，不建独立表；事实带时间有效性（valid_at / invalid_at / expired_at）、来源（episode 链）与用户状态（confirmed / suggested / ignored / banned）。UI 的记忆分组是按 fact type 过滤的视图。
_Avoid_: 记忆库、模式表

**来源（Source）**:
剪贴板条目记录复制时的前台应用（sourceApp），供任务编辑器与候选任务展示来源；只记录，不自动挂接任务。

**切换器（Switcher）**:
按住 Alt 并按 Tab 时浮现的窗口快速切换列表：每项为应用图标 + 应用名（大字）+ 窗口标题（小字），Tab / Shift+Tab / 鼠标循环选中，松开 Alt 切换到高亮项。v1 仅窗口级（无标签页、无搜索），全虚拟桌面枚举。
_Avoid_: 切换面板、应用切换器

**切换器模式（Switcher Mode）**:
Alt+Tab 接管期间面板的整体状态：强制全展开，页面整体替换为切换器，暂停边缘 hover 展开与点击穿透；退出（松开 Alt 或点击某项）后还原触发前状态。
_Avoid_: 切换态、接管模式

## 关系

- 一个任务关联多个应用；多个任务可重叠同一应用。
- 一个关联内容只属于一个任务；内容是剪贴板条目或文件列表。
- 提案确认后：若命中已有任务则并入（merge），否则新建任务（create）。
- 一个任务有多个会话；全局同时只有一个 RUNNING 任务（runningTaskCount ≤ 1，状态控制器强制）。
- 一个会话包含多个活动（1:N）；活动是观察对象，会话是解释对象。
- 活动由聚类从证据时间线（事件流）分析而来；证据时间线持久化的是采集事件，活动本身不持久化。
- 置信度低于阈值、AI 未配置或识别失败时，任务标题回退算法生成（"XX + YY task"）。

## 已澄清的歧义

- "中转站" = 剪贴板栈，无独立概念（2026-08-11 确认）。
- "OCR 内容"只在 AI 管道内存在，UI 不展示 raw 文本（2026-08-11 确认）。
- 备选任务 / 候选任务 / 提案 = 同一物：UI 固定"候选任务"（ADR-0004），代码类型名 TaskProposal（原 Suggestion）（2026-08-12 确认）。
- 本地模型 ≠ Ollama：Ollama 集成已删除，本地模型嵌入产品内部、默认关闭（2026-08-12 确认）。
- 任务状态机：active→RUNNING（唯一）、waiting→WAITING（自动休息态）、paused→PAUSED（手动、免疫自动恢复）、新增 ARCHIVED；旧数据迁移：多 active 只保留最近活跃者为 RUNNING（2026-08-12 确认）。
- Agent 主路径 = 一次性任务决策（evaluateTaskContext）；工具循环只作高不确定场景升级路径（2026-08-12 确认）。
- Agent 预填最小上下文 = 当前活动 + 当前会话 + ≤3 确定性候选 + 已匹配记忆 + 画像，整体过隐私门（不变量 D）；升级路径 = 四固定工具面（search_tasks / search_memories / search_activities / search_clipboard）≤3 次预算，剪贴板仅预览且受开关控制（2026-08-13 确认）。
- new/merge 决策产出 TaskProposal 提案（≤3 未决 FIFO，同目标替换）；ignore 决策记推荐历史（L3 not_now 冷却）；decisionId 决策链贯穿隐私拦截 / 工具召回 / 提案 / trace（2026-08-13 确认）。
- suggestTitle 从 suggestionEngine 迁入决策模块（createTitleSuggester），IPC 通道 task:suggest-title 与触发条件不变（2026-08-13 确认）。
- matchedMemories = 确定性预筛（活动 / 时间 / 实体 / 相关性命中），不是"记忆 Top-K"（2026-08-12 确认）。
- 记忆存储只有 episode / entity / fact 三类表；Profile / Pattern / Task 记忆 / Preference 是 fact 的类型，不是独立实体表（2026-08-12 确认）。
- 证据分级（EvidenceLevel）与隐私敏感度是两个正交维度，不合并成一个整数（2026-08-12 确认）。
- 自动挂接（Attach）与自动归属开关（autoAttributionEnabled）已随 t14 移除；剪贴板事件仍流向引擎做分段，但不再自动 link 到任务（2026-08-12 确认）。
- 切换器交互形态只有按住型（hold）：Alt 按下不响应，按 Tab 才显示；常驻型（persistent）已否决（2026-08-12 确认）。v1 无搜索、无标签页、无风格开关。
