# 53 — 本地模型管理器与运行时

**What to build:** 本地模型的两半：管理器（LocalModelManager）管 registry / 下载 / 校验和 / 路径 / 可用性 / 生命周期（首次启用自动下载带进度 + 手动 .gguf 路径）；运行时（LocalModelRuntime）管加载 / 推理 / 队列 / 超时（worker 线程不阻塞主进程、懒加载）。模型规格：Qwen3-0.6B GGUF Q8_0、/no_think 显式关闭思考、严格 JSON 输出（客户端校验 + 重试）、低 temperature。不捆绑安装包。

**Blocked by:** 32

**Status:** ready-for-agent

- [ ] 下载器：进度回调、失败恢复、校验和不匹配拒绝加载
- [ ] 手动路径与自动下载两种来源均可用
- [ ] 推理队列：并发限制、超时放弃、失败不崩溃
- [ ] worker 线程执行，主进程不阻塞
- [ ] vitest：注入 fake runtime 测试队列 / 超时 / 失败路径（不真下载模型）；typecheck + npm test 全绿

## 参考

spec 实现决策 11（Manager / Runtime 分离、模型规格、获取方式）；handoff「本地模型」。
