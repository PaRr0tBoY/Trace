# 14 — 剪贴板自动归属

**What to build:** 复制内容自动挂到对应任务：剪贴板捕获瞬间取前台进程作为复制来源，匹配任务 AppRef → 自动链接（快照写入 resources）；无匹配任务不链接；多任务共享应用时归属 lastActiveAt 最近者；设置开关默认开，关闭退化为纯手动。clipboard 事件写入事件日志。

**Blocked by:** 11（任务域核心）、12（L0 事件采集）、13（事件归属器）

**Status:** resolved

- [ ] 复制来源 = 捕获瞬间的前台进程（读 12 的最新前台快照，与 ClipboardWatcher 轮询解耦）；匹配任务 AppRef → link + 快照（复用 11 的快照生成）
- [ ] 消歧与 13 一致：多任务共享应用 → lastActiveAt 最近者
- [ ] 无匹配任务 → 不自动链接；归属是规则（非 AI）、可解释（"从 Code 复制 → 任务 X"）
- [ ] 设置开关"剪贴板自动归属"（默认开，三处登记）；关闭后行为退化为纯手动
- [ ] clipboard 事件 `{type:'clipboard', appName, exePath, pid, ts}` 写入事件日志（12 的缓冲）
- [ ] vitest：来源进程匹配、多任务消歧、无匹配不链接、开关关闭不链接；typecheck + npm test 全绿

## 参考

规格《实现决策 4》剪贴板/文件挂接。

## Resolution

Implemented 92ae792 (14 = 14-剪贴板自动归属); wave-verified by coordinator (typecheck + vitest green). t12 additionally got a koffi OpenProcess fix (a5fe873, runtime smoke-test catch). t20 split across t20a (MemoryStore, c307870) + t20b (integration, 7510a92). Final regression: 308 tests pass, build ok, startup smoke clean.
