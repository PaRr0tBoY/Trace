# 49 — 时段整理

**What to build:** 自动提炼事实的整理流程：新活动原始落库为 episode（免费）→ 触发（会话结束 + 时段边界早 6-12 / 中 12-18 / 晚 18-6 + 6h 兜底）→ 整批 combined extraction（节点边提取一次 + 批量时间戳一次，≤2 次 LLM 调用）→ 确定性去重 / 矛盾消解 → 事实入库。提取失败静默降级（不阻塞、无部分写入）。

**Blocked by:** 37, 48

**Status:** ready-for-agent

- [ ] 三种触发各自生效（注入时钟可测）
- [ ] 整批提取调用次数 ≤2（调用计数可注入）；失败不落半成品
- [ ] 提取结果经去重 / 矛盾消解后入库，来源链完整
- [ ] vitest：三触发 / 调用预算 / 失败降级；typecheck + npm test 全绿

## 参考

spec 实现决策 10（combined extraction、触发条件）；graphiti 调研（combined extraction 位置：bulk_utils.py:263-327、extract_timestamps_batch :233-251）。
