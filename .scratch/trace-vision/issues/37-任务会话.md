# 37 — 任务会话 TaskSession

**What to build:** 任务会话作为一等对象：任务每次连续运行记录一个会话（起止时间、置信度、切换原因、前一任务）；一个任务多个会话、全局同时一个会话在运行；会话是其窗口内多个活动的容器（1:N，不一一对应）。现有运行时长（activeMs）聚合语义不变。会话持久化，任务详情可见历史会话。

**Blocked by:** 36

**Status:** ready-for-agent

- [ ] RUNNING 切换 = 旧会话结算（endedAt / 切换原因 / 置信度）+ 新会话开启，一次原子完成
- [ ] 会话落库（task_sessions 表），重启可查；activeMs 聚合结果与改造前一致
- [ ] 任务详情 UI 展示会话历史（起止、原因、前一任务）
- [ ] vitest：会话开/结/切换原子性 + 聚合一致性；typecheck + npm test 全绿

## 参考

spec 实现决策 4（TaskSession 契约：taskId / startedAt / endedAt? / confidence / transitionReason / previousTaskId?）；CONTEXT.md 任务会话词条。
