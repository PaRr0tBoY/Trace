# 23 — 备选任务卡片两行重设计

**What to build:** 备选卡（SuggestionCard）压成两行：第一行完整任务标题（+低置信标记），第二行 APP 图标（不显示名字）+ 操作图标按钮（确认 ✓ / 编辑 ✎ / 忽略 ✕）。去掉 dest 提示行；"为什么"折叠区保留，展开显示算法证据 + LLM 原因。

**Blocked by:** 26（APP 图标）

**Status:** open

- [ ] 第一行：标题（含编辑态 input，Enter 确认 / Escape 取消）+ 低置信 badge
- [ ] 第二行：app 图标组（≤5 个，来自 26 的图标通道，不显示文字名）+ 右侧操作图标按钮（确认/编辑/忽略，hover 显示 tooltip）
- [ ] "为什么"折叠钮收起时一行高度内不显示；展开区内容维持现有（算法证据 + 可选 LLM 原因）
- [ ] 卡片整体高度显著低于现状（两行 + 折叠钮），列表内多张备选卡不占满半屏
- [ ] i18n：新增/修改文案 zh 必填其余回退；typecheck + npm test 全绿；真机：备选卡确认/编辑/忽略全流程

## 参考

SuggestionCard.tsx 现状（5 段结构）、tasks.css .task-suggestion-*、CONTEXT.md 备选任务条目。用户确认：第一页 APP 只显示图标不显示名称，按钮全部图标化。
