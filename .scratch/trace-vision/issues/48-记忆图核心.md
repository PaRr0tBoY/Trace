# 48 — 记忆图核心

**What to build:** 记忆从单表 JSON 迁到三表事实图：episodes / entities / facts + FTS5；画像、模式、任务记忆、偏好一律是 fact 的 type / source 字段，不建独立表；事实带时间有效性（valid_at / invalid_at / expired_at）、来源链（episode 链）、用户状态（confirmed / suggested / ignored / banned）；确定性去重（归一化键 + 余弦 ≥0.6 合并）与矛盾消解（时间有效性冲突写 invalid_at，不覆盖）；权重 = 意图五档 × 现有衰减 × 时段字段；memories.json 一次性迁入（source / userState 不变），迁完删除。

**Blocked by:** 33

**Status:** ready-for-agent

- [ ] 三表落库 + FTS5；事实查询按 type 过滤（UI 分组基础）
- [ ] 去重合并与矛盾消解规则正确（时间冲突不覆盖、写 invalid_at）
- [ ] 权重计算（五档 × 衰减 × 时段字段）
- [ ] memories.json 迁移：数据不丢、来源与用户状态不变，迁完旧文件删除
- [ ] 记忆面板现有功能不回归
- [ ] vitest：提取 / 去重 / 矛盾 / 权重 / 迁移；typecheck + npm test 全绿

## 参考

spec 实现决策 10（三表、无独立表、去重阈值、权重）；记忆存储现状（Memory 类型字段）。
