# 50 — 记忆检索

**What to build:** 预填记忆检索：确定性预筛（活动 / 时间 / 实体 / 相关性命中，不是 Top-K）+ 时间过滤 + 关系扩散（默认 1-hop、上限 2-hop，命中路径与原因记入 trace）。不加 embedding（本地模型 ≠ 嵌入模型）。检索结果供决策预填与工具使用。

**Blocked by:** 41, 48

**Status:** ready-for-agent

- [ ] 确定性预筛命中逻辑（活动匹配 / 时间窗 / 实体命中）
- [ ] 扩散默认 1-hop、2-hop 上限强制；命中路径与 hop 数记入 trace
- [ ] 结果排序 = 权重 × 时间有效性，时间过滤正确
- [ ] vitest：预筛 / 扩散上限 / 路径记录；typecheck + npm test 全绿

## 参考

spec 实现决策 10（检索四要素、hop 上限、不加 embedding）；CONTEXT.md 歧义区（matchedMemories = 确定性预筛）。
