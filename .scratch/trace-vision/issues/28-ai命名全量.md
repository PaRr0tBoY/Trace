# 28 — AI 命名全量 + 手建任务 AI 建议标题

**What to build:** 有 provider 时，建议卡标题全量走 LLM（含高置信 merge 卡），算法标题只作无 AI 时的降级；手建任务（新建/编辑）加"✨ AI 建议标题"按钮，用任务已有信息生成标题供采纳。

**Blocked by:** 无

**Status:** open

- [ ] suggestionEngine：llmCandidates 从"新候选 + 低置信"放宽到全部 built（MAX_LLM_CANDIDATES 上限保留）；LLM 失败时回退算法标题（现状逻辑）
- [ ] 新 IPC `task:suggest-title`（invoke）：renderer 传任务上下文（标题/note/apps/资源 preview）→ main 走 provider 链生成 1-3 个标题候选 → 返回；无 provider 或失败返回 null
- [ ] TaskEditor 增加"✨ AI 建议标题"按钮：点击 → 请求 → 候选填入标题输入框（或直接替换），用户可改可忽略；加载态/失败 toast
- [ ] i18n zh 必填；typecheck + npm test 全绿；真机：无 provider 时按钮禁用或隐藏，有 provider 时生成标题

## 参考

suggestionEngine.ts annotateWithLlm/algorithmicTitle、provider.ts ChatFn、CONTEXT.md AI 命名条目。用户确认：建议卡全量 LLM + 手建任务按钮（2026-08-11）。
