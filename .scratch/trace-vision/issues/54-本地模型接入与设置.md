# 54 — 本地模型接入与设置

**What to build:** 本地模型作为可选增强接入决策前：聚类产出候选后做过滤（≤3）/ 标题草稿 / 排序，输出统一中间结构 CandidateActivity（activityId / candidateTaskId? / semanticLabel? / score / evidenceRefs[]）；关闭或失败自动降级纯算法（功能等价，阶段不变量 H）。设置 UI：开关 / 下载进度 / 路径 + 30 语言文案。

**Blocked by:** 45, 53

**Status:** ready-for-agent

- [ ] CandidateActivity 中间结构定义与产出路径；失败 / 关闭时算法候选原样传递（绝不污染决策数据）
- [ ] 过滤 ≤3 + 标题草稿 + 排序生效（注入 fake runtime 可测）
- [ ] 设置 UI：开关 / 下载进度 / 手动路径，状态持久化
- [ ] 关闭 = 功能等价（不变量 H：本地模型关，其余功能照常）
- [ ] vitest：注入 fake runtime 的可选性测试；typecheck + npm test 全绿

## 参考

spec 实现决策 6 / 11（CandidateActivity 契约、接入点）；用户故事 42 / 43 / 45。
