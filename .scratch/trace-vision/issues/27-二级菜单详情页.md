# 27 — 二级菜单（完整任务卡页面）

**What to build:** 点击任务卡进入全页详情（面板内整页，左上角返回）。内容：APP 图标+名称、进程窗口名、任务内容描述（资源预览）、置信度、创建原因、编辑按钮。OCR/算法 raw 不进 UI，只作 AI 资料（用户确认）。

**Blocked by:** 26（APP 图标）

**Status:** open

- [ ] 模型扩展（shared/types.ts + TaskStore 持久化）：Task 增加 `windowTitles: string[]`（最近窗口名，attributor 已有来源）、`confidence?: number`（创建时来源）、`reason?: string`（创建原因 = LLM 原因或算法证据摘要）；旧数据读取容错（字段缺失不崩）
- [ ] 确认备选时把窗口名/置信度/原因写入任务（suggestionEngine accept 路径扩展）
- [ ] 详情页布局（面板内整页，风格延续）：返回按钮（左上）+ 状态 pill；标题 + 编辑按钮；APP 区（图标+名称，可点开 exePath）；窗口名列表；内容描述（资源预览：文本 preview/图片缩略图/文件路径，alive 占位沿用）；置信度 + 创建原因区（人类可读，无 raw）
- [ ] hover 高亮蒙版（知悉项，与现有 .task-card:hover 一致即可）
- [ ] 任务卡第一页：APP 只显示图标；进入二级菜单后图标+名称
- [ ] i18n zh 必填；typecheck + npm test 全绿；真机：从列表进详情、返回、编辑、删除全流程

## 参考

TaskDetail.tsx 现状、CONTEXT.md 二级菜单条目、用户 2026-08-11 设计确认（3 项内容清单）。
